<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Chat Demo</title>
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <style>
        body {
            margin: 0;
        }

        .left-slide {
            background-color: rgba(148, 97, 251);
            position: fixed;
            right: 0;
            top: 0;
            height: 100%;
            width: 100%;
            max-width: 300px;
            box-shadow: 0 0 10px 0 black;
        }

        .warn {
            opacity: 0%;
            animation-name: warnani;
            animation-duration: 3s;
            cursor: default;
        }

        @keyframes warnani {
            from {
                opacity: 0%;
            }

            50% {
                opacity: 100%;
            }

            to {
                opacity: 0%;
            }
        }

        .chat-input {
            background-color: rgb(148, 97, 251);
            height: 60px;
            position: fixed;
            bottom: 0;
            width: 100%;
        }

        @media (max-width: 768px) {
            .left-slide {
                position: relative;
                height: auto;
                max-width: none;
                width: 100%;
            }
        }
    </style>
</head>

<body>
    <main class="container-fluid">
        <div class="row">
            <!-- Chat Section -->
            <div class="col-lg-9 col-md-8 col-sm-12">
                <%if(id.length==0){%>
                <div class="alert alert-warning m-3 text-center">
                    <strong>Login first to see chats</strong>
                </div>
                <%}else{%>
                <%for(let i=0;i<chatdata.length;i++){%>
                <%if(chatdata[i].uid==id){%>
                <div class="row p-2">
                    <div class="col-12">
                        <div class="p-2 bg-primary rounded-pill shadow text-light float-end" data-aos="zoom-in-left">
                            <%-chatdata[i].message%>
                        </div>
                        <div class="float-end" style="clear: right;" data-aos="zoom-in-left">
                            <small class="text-secondary"><%-chatdata[i].time%></small>
                        </div>
                    </div>
                </div>
                <%}else{%>
                <div class="row p-2">
                    <div class="col-12">
                        <div class="p-2 bg-secondary rounded-pill shadow text-light float-start" data-aos="zoom-in-right">
                            <%-chatdata[i].message%>
                        </div>
                        <div class="float-start" style="clear: left;" data-aos="zoom-in-right">
                            <small class="text-secondary"><%-chatdata[i].time%></small>
                        </div>
                    </div>
                </div>
                <%}%>
                <%}%>
                <%}%>

                <!-- Input Box -->
                <div class="chat-input">
                    <div class="container">
                        <form action="/chat?id=<%-id%>" method="post" class="row g-2">
                            <div class="col-10">
                                <input type="text" class="form-control shadow rounded-pill" name="message" placeholder="Type your message here..." required>
                            </div>
                            <div class="col-2">
                                <%if(id.length!=0){%>
                                <button type="submit" class="btn btn-primary rounded-pill shadow w-100">Send</button>
                                <%}else{%>
                                <button type="submit" class="btn btn-primary rounded-pill shadow w-100" disabled>Send</button>
                                <%}%>
                            </div>
                        </form>
                    </div>
                </div>
            </div>

            <!-- Login Sidebar -->
            <div class="col-lg-3 col-md-4 col-sm-12 left-slide">
                <div class="text-center text-light m-3">
                    <h2>Login</h2>
                </div>
                <div class="m-3 p-3">
                    <form action="/login" method="post">
                        <div class="form-floating mb-3">
                            <input type="email" class="form-control" name="email" id="loginEmail" placeholder="name@example.com" required>
                            <label for="loginEmail">Email address</label>
                        </div>
                        <div class="form-floating mb-3">
                            <input type="password" class="form-control" name="password" id="loginPassword" placeholder="Password" required>
                            <label for="loginPassword">Password</label>
                        </div>
                        <%if(id.length!=0){%>
                        <button type="submit" class="btn btn-primary shadow btn-sm" disabled>Login</button>
                        <%}else{%>
                        <button type="submit" class="btn btn-primary shadow btn-sm">Login</button>
                        <%}%>
                    </form>
                    <%if(warn=="warn"){%>
                    <div class="alert alert-danger warn mt-3 text-center">
                        <strong>This Details is already in use</strong>
                    </div>
                    <%}%>
                </div>
            </div>
        </div>
    </main>

    <!-- Signup Modal -->
    <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLabel">Create New Account</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <form action="/signup" method="post">
                        <div class="form-floating mb-3">
                            <input type="text" class="form-control" name="name" id="signupName" placeholder="Your Name" required>
                            <label for="signupName">Your Name</label>
                        </div>
                        <div class="form-floating mb-3">
                            <input type="email" class="form-control" name="email" id="signupEmail" placeholder="name@example.com" required>
                            <label for="signupEmail">Email address</label>
                        </div>
                        <div class="form-floating mb-3">
                            <input type="number" class="form-control" name="number" id="signupNumber" placeholder="Contact Number" required>
                            <label for="signupNumber">Contact Number</label>
                        </div>
                        <div class="form-floating mb-3">
                            <input type="password" class="form-control" name="password" id="signupPassword" placeholder="Set Password" required>
                            <label for="signupPassword">Set Password</label>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Sign Up</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script>
        AOS.init();
    </script>
</body>

</html>
